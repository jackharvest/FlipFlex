diff --git a/mtkclient/Library/DA/mtk_da_handler.py b/mtkclient/Library/DA/mtk_da_handler.py
index 00ab920..3c8858b 100644
--- a/mtkclient/Library/DA/mtk_da_handler.py
+++ b/mtkclient/Library/DA/mtk_da_handler.py
@@ -16,7 +16,10 @@ from mtkclient.Library.Filesystem.mtkdafs import MtkDaFS
 
 try:
     from mfusepy import FUSE
-except ImportError:
+except (ImportError, OSError):
+    # OSError('Unable to find libfuse') when macFUSE is absent -- same fix as
+    # Filesystem/mtkdafs.py. FUSE is only dereferenced behind an
+    # `if FUSE is not None` at the mount site, so None is safe.
     FUSE = None
 
 
diff --git a/mtkclient/Library/Filesystem/mtkdafs.py b/mtkclient/Library/Filesystem/mtkdafs.py
index 490d216..42abbb3 100644
--- a/mtkclient/Library/Filesystem/mtkdafs.py
+++ b/mtkclient/Library/Filesystem/mtkdafs.py
@@ -78,7 +78,11 @@ try:
                 self.files[path]['st_mode'] &= ~0o222
             return self.files[path]
 
-except ImportError:
+except (ImportError, OSError):
+    # mfusepy raises OSError('Unable to find libfuse'), not ImportError, when
+    # macFUSE is absent -- so the original guard missed it and the whole CLI
+    # died at import. We never mount the DA filesystem; the fallback below is
+    # the author's own no-op and is all we need.
     print('fuse library not installed')
 
     class MtkDaFS():
