# Nothing to keep yet. The API layer uses org.json and HttpURLConnection, both
# platform classes with no reflection, so there are no serialization rules to
# write -- which is a large part of why they were chosen over a JSON library.
